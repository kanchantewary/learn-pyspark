#flask_web/app.py

from flask import Flask

app=Flask(__name__)

@app.route('/')

def hello():
    #return "running flask inside a container"
    return "running flask inside a docker container, hosted in ibm cloud using kubernetes"

if __name__=='__main__':
    app.run(debug=True,host='0.0.0.0',port=int("5000"))
